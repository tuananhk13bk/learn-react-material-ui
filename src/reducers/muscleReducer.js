import { muscles } from '../dataSet'

// muscles is an array
const initState = muscles

const muscleReducer = (state=initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default muscleReducer

